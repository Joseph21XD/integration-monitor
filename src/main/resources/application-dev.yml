spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    show-sql: true

logging:
  level:
    com.integration.monitor: DEBUG
    org.hibernate.SQL: DEBUG